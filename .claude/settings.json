{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "pip -q install --ignore-installed blinker -r requirements.txt >/dev/null 2>&1 || true"
          }
        ]
      }
    ]
  }
}
